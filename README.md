# Agentic AI for Cloud Operations

A natural language agent to manage OpenStack tasks.

## How to Run

```bash
python app.py
```

Or with Docker:

```bash
docker build -t openstack-agent .
docker run -it openstack-agent
```

## Features
- Create, delete, and list instances.
- Natural language interface.
